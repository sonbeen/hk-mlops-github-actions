name: Python application

on:
  push:
    branches: [ test-workflow2 ]
  pull_request:
    branches: [ test-workflow2 ]

jobs:
  build:
    runs-on: ubuntu-latest
   
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: "3.8"
    - name: Display Python version
      run: python -c "import sys; print(sys.version)"

    - name: 아무거나 입력하기
      run: echo "아무거나 입력해용"

      

      
